Proof Statement:
I built BugHunter AI, a Streamlit tool that uses the Gemini API to 
detect bugs in code and return a corrected fix — solo, end to end. 
I tested it against a handful of broken code samples and it 
correctly identified the bug and produced a working fix in each 
case. It's not a finished product yet — explanations and code-
quality review are still unbuilt — but the core piece, getting an 
LLM to actually localize and fix a real bug, already works. I'm a 
5th-semester engineering student building this because I want to 
get good at using AI as a force-multiplier on real problems, not 
just talk about it. If you're a technical hiring manager who wants 
to see how someone actually thinks through a problem rather than 
just what's on a resume, take a look at my work — then reach out.

Why this needs to exist:
A resume can say "used Gemini API." It can't show how I actually 
approached debugging a real fix or the judgment behind it — that's 
what the case study on this site is for.problems.